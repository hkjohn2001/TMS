<h1>Edit User</h1>

<%= semantic_form_for(@user) do |f| %>
 <%= render 'fields', :f => f %>

 <div class="actions">
	<%= f.submit :update %><br />
 </div>

<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
